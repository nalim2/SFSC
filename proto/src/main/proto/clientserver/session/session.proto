syntax = "proto3";

package de.unistuttgart.isw.sfsc.protocol.session;

import "clientserver/session/welcome.proto";

option java_multiple_files = true;

message SessionMessage {
  oneof payload {
    WelcomeMessage welcome_message = 2;
  }
}

